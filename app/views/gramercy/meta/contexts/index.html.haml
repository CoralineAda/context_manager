%h1
  Contexts (#{@contexts.to_a.count})

= link_to "Add Context", new_gramercy_meta_context_path, class: "pull-right btn btn-primary"

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Roots
      %th
  %tbody
    - @contexts.each do |context|
      %tr
        %td
          = link_to context.name, context
        %td
          = context.roots.count
        %td
          .pull-right
            = link_to 'Destroy', context, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-xs'